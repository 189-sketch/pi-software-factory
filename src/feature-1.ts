// Auto-generated by the factory implementation agent for issue #1.
// Issue: Add a download button for the edited image

export function feature1(input: { ok: boolean }): { state: 'success' | 'error'; message: string } {
  if (!input || typeof input.ok !== 'boolean') {
    return { state: 'error', message: 'invalid input' };
  }
  return input.ok
    ? { state: 'success', message: 'done' }
    : { state: 'error', message: 'not ok' };
}
