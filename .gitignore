node_modules/
.factory/
.env
dist/
*.log
*.tsbuildinfo

# Build / bundle output
bin/*.js
!bin/factory.js
!bin/factory-panel.js

# Runtime state — generated by the daemon, not source
factory/state/

# Local scratch / dev artefacts
.playwright-mcp/
evidence/
pr_description.txt
pr_diff.txt
review.json
fleet-screenshot.png
fleet.png

# npm auth token + tarball artifacts (never commit)
.npmrc
*.tgz